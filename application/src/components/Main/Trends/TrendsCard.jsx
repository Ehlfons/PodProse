import {Fragment} from 'react'

const TrendsCard = () => {
  return (
    <Fragment>
      {/* Función que recorra el objeto de categorías, saque 5 categorias aleatorias (cada fila será una categoría) y las muestre.
        * Además, tendrá que contar el numero de podcast que tiene esa determinada categoría y mostrarlo.
        * Además, tendrá que mostrar bajo la descripción de las categorías seleccionadas.
        * Selccionar 3 podcasts aleatorios de su categoría y mostrarlos a la derecha.
      */}
    </Fragment>
  )
}

export default TrendsCard