/* import useListas from "../hooks/useListas.jsx";
import "./Modales.css"; */

function DeletePodcastModal({ mostrar, manejarCerrado, idPodcast, idListaActual }) { 
  /* const { deletePodcastLista } = useListas(); // Importado desde el contexto a través del hook usePodcasts.

  // Función para manejar el borrado del producto.
  const manejarBorrado = () => {
    // Llama a la función deletePodcast con el ID del producto y el ID de la lista.
    deletePodcastLista(idPodcast, idListaActual);
    
    // Cierra el modal después de eliminar el producto.
    manejarCerrado();
  }; */

  return (
    <>
      
    </>
  );
}

export default DeletePodcastModal;